# Core engine needs only PyYAML. The web app adds streamlit + pandas.
PyYAML>=6.0
pytest>=7.0
streamlit>=1.30
pandas>=2.0

# EDA Assistant (eda_assistant_app.py). 데이터 진단·추천·전처리·시각화·보고서.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3   # PCA/클러스터링/IsolationForest/LOF (없어도 IQR·zscore 로 폴백)
plotly>=5.18        # 추천 시각화(Histogram, Heatmap, 3D Surface, Radar 등)
